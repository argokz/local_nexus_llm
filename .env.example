# Copy to .env and edit. Master key MUST start with sk-
LITELLM_MASTER_KEY=sk-localnexus-admin
POSTGRES_USER=nexus
POSTGRES_PASSWORD=nexus
POSTGRES_DB=nexus

# Bound on the host for LAN clients. 0.0.0.0 = all interfaces.
LITELLM_PORT=4000

# Embedding upstream LiteLLM talks to.
# Default sandbox: llama.cpp (works on i7-2600).
# Production-like: http://tei:80/v1  after `docker compose --profile tei up -d`
EMBED_API_BASE=http://embed:8003/v1

# Set to 99 when a CUDA GPU appears. Keep 0 on this machine.
LLAMA_NGL=0
LLAMA_THREADS=4
LLAMA_CTX=4096
